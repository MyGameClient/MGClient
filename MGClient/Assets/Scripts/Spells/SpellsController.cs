using UnityEngine;
using System.Collections;

//public class Spell
//{
//	public float distance = 400;
//	public float spd = 1500;
//}

public class SpellsController : MonoBehaviour {

	/*public Spell spell = new Spell ();

	private Unit unit;

	void Start () 
	{
		unit = GetComponent <Unit>();	
	}

	#region assault
	public void assault ()
	{
		float dir = MGMath.getDirNumber (unit);
		unit.MoveToTarget (transform.position + new Vector3 (dir * spell.distance, 0, 0), spell.spd);

	}
	#endregion

	#region DEBUG
	public int max = 1;
	void OnGUI ()
	{
		for (int i = 0; i < max; i++)
		{
			if (GUI.Button (new Rect (100 * i,Screen.height - 100, 100, 100), "spell" + i.ToString()))
			{
				test (i);
			}
		}
	}
	void test(int i)
	{
		if (i == 0)
		{
			assault ();
		}
	}
	#endregion*/
}
